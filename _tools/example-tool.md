---
title: "harness-gen"
description: >-
  A small Python utility that scaffolds AFL++ fuzzing harnesses from a
  function signature, saving the boilerplate every new target needs.
language: "Python"
---

Replace this with a real description of your tool, and link to its
GitHub repo. Like the Research collection, this file automatically
gets its own page since `output: true` is set for the `tools`
collection in `_config.yml`.
