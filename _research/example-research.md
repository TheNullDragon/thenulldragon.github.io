---
title: "Use-After-Free in libexample's Buffer Pool"
description: >-
  Analysis of a use-after-free condition in a common buffer-pooling
  library, including a proof-of-concept and suggested mitigation.
status: "CVE Pending"
---

Replace this file's content with your full research write-up, or link
out to a PDF / GitHub repo. This file is not rendered on its own page
by default — it currently just feeds the tile on the Research page.
If you want individual research pages, give this file a `permalink`
and it will get one automatically (the collection config in
`_config.yml` already sets `output: true`).
